{
  "name": "@diego22rct/tenets",
  "version": "0.4.1",
  "description": "Static analysis CLI for SOLID/DRY/KISS/YAGNI violations in HonoJS + Next.js codebases",
  "license": "MIT",
  "type": "module",
  "bin": {
    "tenets": "dist/cli/index.js"
  },
  "exports": {
    ".": "./dist/core/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=16.20.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ignore": "^7.0.6",
    "typescript": "^7.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "vitest": "^2.1.4"
  }
}
